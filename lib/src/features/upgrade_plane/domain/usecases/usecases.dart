 
    import '../repositories/repositories.dart';

    class GetUpgrade_planeUseCase {
        final Upgrade_planeRepository repository;
      
        GetUpgrade_planeUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      